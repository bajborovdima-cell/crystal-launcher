name: Build

on:
  push:
    branches: [main]
    tags: [v*]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            flags: --win portable --config.win.sign=false
          - os: ubuntu-latest
            flags: --linux
          - os: macos-latest
            flags: --mac
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - working-directory: minecraft-launcher
        run: npm ci
      - name: Build
        working-directory: minecraft-launcher
        run: npx electron-builder ${{ matrix.flags }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/upload-artifact@v4
        with:
          name: CrystalLauncher-${{ matrix.os }}
          path: minecraft-launcher/dist/*.{exe,AppImage,dmg,deb}
